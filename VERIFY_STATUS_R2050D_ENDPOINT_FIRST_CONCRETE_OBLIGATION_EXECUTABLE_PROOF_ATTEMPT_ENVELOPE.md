# Verify Status: R2050D Endpoint First Concrete Obligation Executable Proof Attempt Envelope

Expected verification command:

```bash
rm -rf .lake
export PATH="$HOME/.elan/bin:$PATH"
lake build 2>&1 | tee VERIFY_LOG_CODESPACES_R2051D_ENDPOINT_FIRST_CONCRETE_OBLIGATION_ATTEMPT_TRACE_LEDGER.txt
```

Expected result after applying R2050D/R2051D together:

```text
Build completed successfully (513 jobs).
```
