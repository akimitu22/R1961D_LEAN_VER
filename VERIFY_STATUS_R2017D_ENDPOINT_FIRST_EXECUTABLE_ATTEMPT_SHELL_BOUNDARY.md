# VERIFY STATUS — R2017D endpoint first executable-attempt shell boundary

Status: prepared for Codespaces verification.

Expected result:

```text
Build completed successfully (... jobs).
```

Expected job count: about 343 jobs. Stop if fewer than 333 jobs are built.
