# R2017D endpoint first executable-attempt shell boundary ledger

R2017D refines R2016D validation slots into an executable-attempt shell boundary.

Center layer: `R2017DLeanEndpointFirstExecutableAttemptShellBoundary`

Center theorem:

```lean
R2017DLeanEndpointFirstExecutableAttemptShellBoundary.endpoint_first_executable_attempt_shell_boundary_to_actual_li_target
```

The layer records the shell boundary for a later executable attempt. It does not start the attempt or execute the endpoint proof kernel.
