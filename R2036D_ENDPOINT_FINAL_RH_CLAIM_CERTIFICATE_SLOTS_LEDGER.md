# R2036D endpoint final RH-claim certificate slots ledger

Adds `R2036DLeanEndpointFinalRHClaimCertificateSlots`.

Primary formal interface:

```lean
R2036DLeanEndpointFinalRHClaimCertificateSlots.endpoint_final_rh_claim_certificate_slots_to_actual_li_target
```

This layer records certificate slots for the final RH-claim boundary. It does not prove RH, the final theorem, the explicit formula, or endpoint estimate.
