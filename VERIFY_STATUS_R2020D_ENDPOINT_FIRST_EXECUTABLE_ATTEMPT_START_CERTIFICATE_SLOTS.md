# VERIFY_STATUS R2020D endpoint first executable-attempt start-certificate slots

Status: prepared for GitHub Codespaces verification.

Expected top target after applying the combined patch:

```text
R2021DLeanEndpointFirstProofKernelInvocationBoundary
```

Expected build result: `Build completed successfully (... jobs)` with at least 353 jobs and normally about 363 jobs for this two-layer continuation.
