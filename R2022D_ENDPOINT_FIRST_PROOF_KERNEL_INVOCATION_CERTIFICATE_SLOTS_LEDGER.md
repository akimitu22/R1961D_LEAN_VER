# R2022D endpoint first proof-kernel invocation certificate slots ledger

Center layer: `R2022DLeanEndpointFirstProofKernelInvocationCertificateSlots`.

Center theorem:

```lean
R2022DLeanEndpointFirstProofKernelInvocationCertificateSlots.endpoint_first_proof_kernel_invocation_certificate_slots_to_actual_li_target
```

This layer records proof-kernel invocation certificate slots. It does not invoke or execute the endpoint proof kernel and does not discharge a concrete endpoint lemma.
