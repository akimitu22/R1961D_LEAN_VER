# R1970D Ninth Best Step

The next best step after R1969D is to stop refining only the meta-ledger and fix
a single smallest analytic entry point for actual formalization work.

R1970D selects the Abel-boundary definition target as the minimal first target.
This choice is narrower than the full zeta function, narrower than the full
explicit formula, and narrower than packet estimates.  It is still connected to
the actual Li-boundary chain through the evidence-slot, anchor, source-category,
realization-interface, micro-target, analytic-decomposition, and packet-balance
layers.

The intended bridge is:

```text
minimal first formalization target
→ formalization priority plan
→ anchor classification
→ source evidence slots
→ concrete source categories
→ realization interfaces
→ micro-targets
→ analytic decomposition slots
→ analytic realization obligations
→ R1961D packet-balance layer
→ actual Li target
```

This file is a planning certificate, not an analytic proof certificate.
