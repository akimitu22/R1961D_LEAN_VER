# R1956D mathlib-facing anchors

The R1956D anchors are declared connection points only. They are not proofs that the manuscript objects have already been connected to mathlib's analytic number theory.

- Riemann zeta object anchor.
- Li criterion target anchor.
- Explicit formula package anchor.
- Scale topology and uniform estimates anchor.

The Lean file `R1956DLeanInterface/MathlibAnchors.lean` explicitly records `currentlyFormalizedHere := false` for these anchors.
