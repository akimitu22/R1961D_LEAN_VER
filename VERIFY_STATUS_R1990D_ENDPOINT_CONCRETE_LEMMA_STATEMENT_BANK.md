# Verify status: R1990D endpoint concrete lemma statement bank

Expected command:

```bash
lake build 2>&1 | tee VERIFY_LOG_CODESPACES_R1990D_ENDPOINT_CONCRETE_LEMMA_STATEMENT_BANK.txt
```

Success condition:

```text
Build completed successfully (... jobs).
```

This patch is not locally lake-verified in the artifact-generation environment.
