# R1996D Endpoint Executable Kernel Boundary Ledger

## Role

R1996D refines the R1995D endpoint first executable skeleton into an endpoint
executable kernel boundary and dependency plan.

## Center layer

- `R1996DLeanEndpointExecutableKernelBoundary`
- Center bridge theorem:
  `R1996DLeanEndpointExecutableKernelBoundary.endpoint_executable_kernel_boundary_to_actual_li_target`

## Non-claim boundary

This layer records boundary objects for a later executable endpoint kernel file.
It does not implement or execute an endpoint proof kernel, discharge an endpoint
estimate, prove an explicit formula, or prove RH.

## Import-spine guard

The aggregate file explicitly imports:

```lean
import R1995DLeanEndpointFirstExecutableSkeleton
```

so the R1995D aggregate spine remains reachable from the R1996D default target.
