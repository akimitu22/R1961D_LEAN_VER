# R2021D sixtieth best step

Best step: add endpoint first proof-kernel invocation boundary.

This is the next boundary after executable-attempt start-certificate slots. It prepares the proof-kernel invocation interface while keeping all substantive execution and lemma discharge claims outside the current layer.
