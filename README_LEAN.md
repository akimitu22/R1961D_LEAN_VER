# R1995D Lean patch

Adds `R1995DLeanEndpointFirstExecutableSkeleton`, preserving the aggregate import spine from R1994D.


## R1996D Endpoint Executable Kernel Boundary

Adds `R1996DLeanEndpointExecutableKernelBoundary`, refining the R1995D endpoint
first executable skeleton into an executable kernel boundary and dependency
plan.  This is still a non-claim layer: no endpoint proof-kernel execution and
no analytic estimate discharge are performed here.
