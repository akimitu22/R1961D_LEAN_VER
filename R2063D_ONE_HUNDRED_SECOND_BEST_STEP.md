# R2063D — One-hundred-second best step

Layer: `EndpointFirstConcreteObligationLocalDischargeAttempt`.

Purpose: use the R2062D witness kernel to expose the first concrete local-discharge attempt. The layer states the conditional route to local discharge and records the missing witness, endpoint estimate, explicit formula, transfer assembly, local-discharge certificate, and checker-kernel execution obligations as Lean-level objects.

Proof-consistency note: this is an attempt/obstruction layer, not a completed proof. It avoids hiding failure behind another interface and does not claim RH or a completed endpoint proof.
