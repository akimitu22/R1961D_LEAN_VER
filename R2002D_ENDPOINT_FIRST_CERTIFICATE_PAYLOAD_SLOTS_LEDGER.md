# R2002D endpoint first certificate payload slots ledger

R2002D refines the R2001D first discharge certificate interface into endpoint
first-certificate payload slots and a payload dependency plan.

Non-claim boundary:
- no payload term is supplied here;
- no proof certificate is supplied here;
- no concrete endpoint lemma is discharged here;
- no endpoint proof kernel is executed here;
- no endpoint estimate, explicit formula, or RH statement is proved here.
