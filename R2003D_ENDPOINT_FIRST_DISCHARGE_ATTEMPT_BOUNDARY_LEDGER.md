# R2003D endpoint first discharge attempt boundary ledger

R2003D refines the R2002D first-certificate payload slots into a first discharge
attempt boundary and dependency audit.

Non-claim boundary:
- no discharge attempt is started here;
- no payload/proof certificate is supplied here;
- no concrete endpoint lemma is discharged here;
- no endpoint proof kernel is executed here;
- no endpoint estimate, explicit formula, or RH statement is proved here.
