# R1995D Endpoint First Executable Skeleton Ledger

## Purpose

R1995D refines the R1994D endpoint proof-kernel execution interface into a first executable skeleton and dependency plan.

## Boundary

This layer records the executable skeleton interface only. It does not implement or execute the endpoint proof kernel, does not close endpoint estimates, and makes no RH-level claim.

## Center theorem

`R1995DLeanEndpointFirstExecutableSkeleton.endpoint_first_executable_skeleton_to_actual_li_target`

## Main import spine

The aggregate file explicitly imports `R1994DLeanEndpointProofKernelExecutionInterface` to preserve the verified aggregate spine.
