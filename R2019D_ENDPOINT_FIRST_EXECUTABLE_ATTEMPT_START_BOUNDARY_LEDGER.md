# R2019D endpoint first executable-attempt start boundary ledger

R2019D refines R2018D attempt input slots into a start-boundary record.

Center layer: `R2019DLeanEndpointFirstExecutableAttemptStartBoundary`

Center theorem:

```lean
R2019DLeanEndpointFirstExecutableAttemptStartBoundary.endpoint_first_executable_attempt_start_boundary_to_actual_li_target
```

The layer records the boundary for a later executable-attempt start. It does not start the attempt or execute the endpoint proof kernel.
