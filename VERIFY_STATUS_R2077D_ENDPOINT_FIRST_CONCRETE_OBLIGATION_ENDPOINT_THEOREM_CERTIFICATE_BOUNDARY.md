# Verify Status R2077D

Expected explicit build target:

```bash
lake build R2077DLeanEndpointFirstConcreteObligationEndpointTheoremCertificateBoundary
```

Success requires `Build completed successfully` in Codespaces.
