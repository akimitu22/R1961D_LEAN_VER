# R2004D Endpoint First Discharge Attempt Input Slots Ledger

R2004D adds endpoint first-discharge attempt input slots and a dependency plan.

Center layer: `R2004DLeanEndpointFirstDischargeAttemptInputSlots`

Center theorem:

```lean
R2004DLeanEndpointFirstDischargeAttemptInputSlots.endpoint_first_discharge_attempt_input_slots_to_actual_li_target
```

Boundary: no discharge attempt is started, no endpoint proof kernel is executed, no concrete endpoint lemma is discharged, and no RH/explicit formula claim is made.
